#!/usr/bin/env bash
# 단원 종합 평가 u1(과학과 인류의 지속가능한 삶) 문항용 실사 사진 15장.
# codex auth의 ChatGPT image_gen 사용(순차 2배치, 병렬 금지). bash qa/order-examu1.sh  (app 루트에서)
set -u
cd "$(dirname "$0")/.."
mkdir -p public/exam/u1

echo "=== BATCH 1/2: exam/u1 사진 [0]~[7] (밝기 다른 화분·측정 도구·씨앗 접시·경사면 공·옛 현미경·증기 기관차·백신 병·위성 안테나) ==="
codex exec --skip-git-repo-check -s danger-full-access -C "$(pwd)" - <<'PROMPT'
qa/examu1_prompts.txt 파일을 읽어라. [0][1][2][3][4][5][6][7] 여덟 개의 이미지 프롬프트가 있다(중1 과학 '과학과 인류의 지속가능한 삶' 시험 문항용 실사 사진).
각 프롬프트 앞에 파일 위쪽 "스타일 블록 A"를 붙여 내장 image_gen 도구로 이미지를 생성하라.
Google 도구 금지, 반드시 내장 image_gen만 사용. 종횡비 전부 정사각 1:1.
저장 경로는 각 프롬프트의 file= 에 적힌 그대로. 이미지 안에 글자·숫자·알파벳·로고 절대 금지.
사람·손·얼굴·신체 금지(반사된 모습 포함). 계기·표시창은 반드시 빈 패널(눈금·숫자 각인 금지).
[0][2][3]은 "무엇이 다른가"가 채점 기준이니 프롬프트에 적힌 좌우 차이(밝기·싹 길이·경사)를 반드시 뚜렷하게 그려라.
각 이미지 후 "IMG i: SAVED <경로>"를 출력하고, 끝나면 "DONE n/8"을 출력하라.
PROMPT

echo "=== BATCH 2/2: exam/u1 사진 [8]~[14] (스마트 스피커·증강 현실 태블릿·스마트홈·태양광·풍력·전기차 충전·업사이클 가방) ==="
codex exec --skip-git-repo-check -s danger-full-access -C "$(pwd)" - <<'PROMPT'
qa/examu1_prompts.txt 파일을 읽어라. [8][9][10][11][12][13][14] 일곱 개의 이미지 프롬프트가 있다(중1 과학 '과학과 인류의 지속가능한 삶' 시험 문항용 실사 사진).
각 프롬프트 앞에 파일 위쪽 "스타일 블록 A"를 붙여 내장 image_gen 도구로 이미지를 생성하라.
Google 도구 금지, 반드시 내장 image_gen만 사용. 종횡비 전부 정사각 1:1.
저장 경로는 각 프롬프트의 file= 에 적힌 그대로. 이미지 안에 글자·숫자·알파벳·로고 절대 금지.
사람·손·얼굴·신체 금지(반사된 모습 포함). 계기·표시창은 반드시 빈 패널(숫자·아이콘 각인 금지).
[9]는 "실제 방은 비어 있는데 화면에만 가구가 겹쳐 보인다"가 채점 기준이니 그 대비를 반드시 뚜렷하게 그려라.
각 이미지 후 "IMG i: SAVED <경로>"를 출력하고, 끝나면 "DONE n/7"을 출력하라.
PROMPT

echo "=== 발주 완료 → node qa/process-geo.mjs 로 webp 변환 ==="
