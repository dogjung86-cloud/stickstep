#!/usr/bin/env bash
# 비교 문항 2쌍의 기준 이미지를 직접 참조해 구도 일치를 보정한다.
# 원본 16장 발주가 끝난 뒤에만 단독 실행한다.
set -u
cd "$(dirname "$0")/.."

codex exec --skip-git-repo-check -s danger-full-access -C "$(pwd)" - <<'PROMPT'
중1 과학 II 시험의 비교 이미지 2장을 순서대로 보정한다. 다른 codex/image_gen 작업과 병렬 실행하지 마라.
각 단계에서 기준 PNG를 먼저 이미지 보기로 확인하고, 반드시 내장 image_gen의 이미지 편집으로 그 기준 파일을 참조한다.
새 그림을 독립 생성하지 말고 카메라 위치, 프레이밍, 물체 크기, 배경, 조명을 최대한 고정한다.
모든 출력은 정사각 1:1이며 글자·숫자·라벨·기호·로고·워터마크가 없어야 한다.

1) 기준 `public/exam/u2/coverslip-bubble.png`를 편집한다. 받침유리, 덮개유리, 액체, 그림자, 구도를 픽셀 수준으로 유지하고 덮개유리 아래의 원형 공기 방울만 자연스럽게 제거한다. 결과를 `public/exam/u2/coverslip-clean.png`에 덮어쓴다.

2) 기준 `public/exam/u2/forest-road.png`를 편집한다. 같은 숲, 같은 중앙 2차선 도로, 같은 카메라와 조명을 유지하고 도로 위에 토종 관목과 작은 나무로 덮인 넓은 생태통로 하나를 추가한다. 생태통로는 양쪽 숲을 분명히 잇고, 기준 이미지의 고라니는 그 통로 위를 걷게 한다. 차량·사람·표지판은 넣지 않는다. 결과를 `public/exam/u2/forest-corridor.png`에 덮어쓴다.

각 결과를 저장한 직후 다시 이미지 보기로 열어 기준과 구도가 일치하는지, 과학적으로 이상한 신체·글자·워터마크가 없는지 확인한다. 실패하면 해당 단계만 다시 편집한다. 각 성공 시 `REPAIRED <path>`를 출력한다.
PROMPT
