# design/ — 홈 리디자인 시안 (2026-07-12 확정)

앱 코드가 아니라 **디자인 결정 기록**이다. 빌드에 포함되지 않는다(public/·src/ 밖).

## 작업물

- `home-redesign-mockup-v2.html` — 홈(레슨 지도) 리디자인 목업 v2.
  브라우저로 바로 열면 된다(자체 완결 HTML, 인터랙티브: 노드 프레스·스틱맨 걷기 데모 포함).
- 게시본(아티팩트, 같은 내용): https://claude.ai/code/artifact/1c0916d8-f60c-46fd-a670-2fdd235f8cdb
  (구 URL 5c6ded30…은 삭제돼 2026-07-13 v2.3판으로 재게시 — 폰 확인용)

## 확정된 방향 (사용자 확정 2026-07-12)

1차 개편 묶음 = **A+B 격상판**:
- **발자국 트레일**: 걸어온 구간의 연결선을 잉크 발자국으로(갈 길은 옅게).
  serpentine 경로 좌표를 그대로 샘플링해 도장 — 목업의 stamp() 함수가 구현 참고.
- **스틱맨 워커**: 현재 노드 위에 내 아바타 스틱맨(깃발). 레슨 완료 귀환 시 다음 노드까지
  걷기(8fps 두 포즈 플립북, 스플래시 125ms 리듬) + 발자국 스탬핑 + 깃발 플랜팅.
  **품질 게이트: 발주본(스플래시·만화) 옆에 놓고 위화감 없을 것 — 통과 전엔 지도에 안 올린다.**
  게이트가 늦으면 발자국(A)만 먼저 출시 가능(캐릭터 리스크 0).
- **눌리는 3D 스텝 노드**(듀오링고식): 노드에 아랫단(엣지) + 프레스 시 윗면 6px 침하.
  저빈도(레슨 진입)라 "고빈도 바운스 금지" 모션 규칙과 충돌 없음. 햅틱 탭(8)과 세트.
  **→ 2026-07-13 변형 탐색 중: 노드 실루엣을 발자국(장화 밑창)으로** — 아래 "진행 중 탐색" 섹션.
  확정되면 이 항목을 교체할 것(원형+상시 아랫단은 듀오링고 서명이라는 우려에서 출발).
- **대단원 탭 = 기존 "로마숫자. 단원명" 유지**(2026-07-14 사용자 재명시로 키워드 필 검토안 폐기.
  2026-07-12의 "로마숫자 보류"는 로마숫자 단독 필에 대한 판정이었고, 기존 풀네임 탭은 그대로 간다):
  긴 탭은 가로 스크롤 + 단원 전환 시 활성 탭 자동 스크롤-인. 카드도 기존 밴드 형태("대단원 Ⅰ"
  eyebrow+제목+설명+정복률) — 상단 구조는 기존 그대로 두고, 카드만 캐러셀(스와이프)화가 개편 내용.
- **앱바**: 그리드 아이콘 → "과학 ▾" 과목 칩(현재 과목 표시 + 과목 허브 진입 겸용).
  우상단 프로필 버튼은 마이 탭(goTab("my"))으로 통일(현재는 로그인 화면으로 가는 개편 전 잔재).
  **→ 2026-07-14 재탐색(사용자 2차 피드백)**: ① 칩이 답답한 원인은 박스(한 줄에 필 셋) —
  "과학 ▾" 칩/텍스트안은 **제자리 드롭다운을 암시해 목적지(화면 전체 과목 허브)와 신호 불일치**로
  폐기. ② **목적지는 기존처럼 전체 화면 과목 허브 유지**(사용자 판정) — 저빈도 큰 전환·"준비 중"
  과목 노출·기구현 화면 재사용. ③ 트리거 = 기존 왼쪽 상자 유지하되 그리드 대신 **현재 과목
  아이콘(과학 플라스크·수학 π)+미니 ▾**(듀오링고 국기 패턴, 목업 폰 적용·trybox 4안 비교) —
  "현재 과목 안 보임" 문제를 그리드 복귀 없이 해결. 순수 그리드 유지도 허용 옵션. 확정 대기.

보류/별도 결정: C(징검다리 리스킨 — '밟는' 감각은 노드 버튼화가 절반 흡수), D(공책 두들 전면 리브랜딩).

## 진행 중 탐색 (2026-07-13) — 노드 실루엣을 발자국(장화 밑창)으로

사용자 우려("원형+상시 아랫단은 듀오링고 짭으로 보일 수 있다")에서 출발. 1차 반응 긍정("나쁘진
않네")이지만 **아직 확정 아님** — 다음 세션은 목업을 보여주고 확정받은 뒤 위 확정 묶음 3번을 갱신할 것.

- **목업 v2에 적용 완료**: 폰 데모 지도 노드 4개가 밑창 노드로 교체돼 있고(defs `#bsole` + use,
  프레스·걷기·상태 전환 전부 동작), "변형 탐색" trybox에 상태 견본 5종(열림·완료·잠금·좌우 기울임 쌍).
  원형 3D 버튼 박스는 "비교용 이전 안"으로 남김.
- **확정된 세부(사용자 지시)**: 발끝은 진행 방향(아래) — 위를 향하면 느낌표(!)로 읽힘. 맨발가락형
  금지, 장화 밑창(앞창+뒤꿈치 2조각) — 장화 레벨(core/level.ts BOOT_TIERS)과 세계관 직결.
- **간격·스크롤 실측**: 노드 중심 좌표를 원형과 동일하게 쓰면 지도 높이·스크롤 총량 불변(데모 중심
  150,58 / 85,170 / 195,270 / 110,372 — 원형과 동일). 노드가 세로 18px 길어 사이 여백만 줄어듦 —
  답답한지 여부만 눈 판정 항목.
- **스틱맨은 패드 위에 선다**: 발 위치 = 노드 중심 +8px(패드 중심), 걷기 경로 하향 보정
  (walkpath M85,178 → 195,278). 트레일 발자국 도장도 발끝이 진행 방향이라 노드와 한 문법.
- **구현 함정(목업에서 실제로 밟은 것)**: ① `.p-map svg` 전역(absolute·inset:0·100%) 규칙이 노드
  svg를 0×0으로 만든다 — `.bsn svg`에 position:static·width:auto 해제 필수(워커 svg와 같은 함정).
  ② 프레스 6px 침하 transform은 `.bs-top` 그룹 CSS에만 — use의 attribute transform과 섞으면 CSS가
  덮어씀(방향 회전은 `#bsole` 내부 rotate(180 24 35)가 소유). ③ 아이콘·금별 배지는 도형과 분리된
  그룹이라 발끝 방향과 무관하게 항상 정립.
- **2026-07-13 2차 반영(사용자 지시 — "실제 사람이 걷는 것처럼")**: ① 지도 노드에 걸음 스플레이 회전 —
  지그재그 바깥쪽으로 +6/−8/+8/−6°(왼발 반시계·오른발 시계, 보행의 발 벌림). 회전은 버튼 inline
  style transform이 소유(프레스 침하는 .bs-top이라 충돌 없음), 아이콘·금별 배지는 inline counter-rotate로
  정립 유지(위 ③ 원칙 계승). ② 트레일 도장 #fp를 맨발(타원+발가락 점 3개)에서 **노드 #bsole 축소판**
  (앞창+뒤꿈치 2조각, s≈0.16)으로 교체 — "발자국 같지 않다" 피드백 해소, 노드와 한 문법. 보폭 16→21px
  (발 길이 11의 ~2배)·좌우 발 벌림 ±6~7° 추가(stamp/stampAt 회전에 side*θ). 걸어온 트레일 끝은
  L−55에서 멈춘다(노드 위에 선 워커 머리·깃발과 도장 겹침 방지 — 눈검수 적발). ③ 워커 스틱맨 1.13×
  확대(.walker-wrap svg CSS scale, transform-origin 23px 62px = 발 앵커 고정) + 귀여움 보강: 머리
  r7→7.7(치비 비율)·눈 확대(r.95→1.1, 중심선 살짝 아래)·둥근 미소·볼터치(#FF9D94 op .5), 선은
  2.6→2.4로 가늘게 유지(확대 후 실질 굵기 유사 — "선 너무 굵으면 어색" 판단 반영).
- **2026-07-13 3차 반영(사용자 지시 + 판단 위임)**: ① 다음 레슨 노드의 밑창 라인(bs-halo)이 숨 쉬듯
  펄스(haloPulse 1.9s opacity+scale, reduced-motion은 정지 링 유지). ② **갈 길 점선 폐기 → 옅은 미니
  밑창 발자국(fp-faint)** — "점선 빼는 게 좋을까?" 판단 위임에 대한 결론: 완전 제거는 길 구조를 잃어서,
  확정안 A의 "갈 길은 옅게" 원안으로 통일. 걷기 데모에서 걷는 구간의 옅은 발자국이 페이드아웃되고 진한
  잉크가 덮는다(fsegA/B 경로 분할 + stamp()가 path._st로 도장 추적). ③ **히어로 카드 기존 형태 복귀**
  (로마숫자+제목+설명+정복률+바 — 슬림 카드 폐기, 사용자 지시) + **카드 좌우 스와이프로 단원 전환**:
  키워드 탭·페이지 dots 동기화, 세로 제스처는 스크롤에 양보(축 잠금), 끝 단원 러버밴딩, 목업엔 Ⅰ·Ⅱ
  지도 + Ⅲ~Ⅶ placeholder(단원 테마 그라데이션은 임시 근사색 — 실구현은 tokens 단원 그라데이션).
  ④ 단원 장식(mapDecor)은 불변 유지(사용자 확인 "그거 좋았거든") — 목업에 견본 삽입(Ⅰ 야자수·징검돌,
  Ⅱ 고사리·물고기), Ⅱ 지도의 워커 깃발은 단원 테마색(bio 그린) 디테일.
- **2026-07-14 4차 반영(사용자 지시)**: ① "로마숫자.단원명"은 카드가 아니라 **탭**에 대한 지시였음
  (사용자 재명시로 보정) — 탭을 기존 unit-tab 문법(`${roman}. ${title}`) 그대로 유지하고 키워드 필
  폐기, 카드는 기존 밴드 형태("대단원 Ⅰ" eyebrow+제목)로 복귀. 긴 탭은 #v2tabs 가로 스크롤(스크롤바
  숨김) + go()가 활성 탭 scrollIntoView(inline center).
  ② 단원 전환을 콘텐츠 스왑이 아니라 **진짜 카드 캐러셀**로 재구현(사용자 "다음 카드가 살짝
  보여야" — 콘텐츠 스왑은 목업 지름길이었음을 인정하고 재작업): 카드 7장이 .ustrip에 실제로 늘어서
  활성 카드 중앙 정렬, 이웃 카드가 양옆 ~16px 피크, 드래그는 손가락 1:1 추적(가로축 잠금·세로는
  스크롤에 양보·끝 러버밴딩 .32·전환 중 그랩은 computed matrix로 현재 위치 승계), 비활성 카드
  scale .955+dim, 활성 dot 알약형(12px). 피크 카드 탭 = 해당 단원 점프. 키워드 탭 유지(카드가
  "Ⅰ. 풀네임"을 소유하므로 탭과 중복 없음). 카드·dots는 JS가 UNITS 데이터로 생성.
  **구현 함정: setPointerCapture를 pointerdown에서 걸면 click이 캡처 대상으로 디스패치돼 피크 카드
  탭이 죽는다** — 캡처는 가로축 확정 순간(첫 유효 pointermove)에만 건다(실탭 e2e로 적발·수정).
  ③ 밑창 면의 유광 하이라이트(흰 타원 fill #fff op .2, cx21 cy33) **전량 제거** — 파운드리 키라이트
  문법을 옮긴 것이었으나 48px 버튼에선 사용자가 "그림자/얼룩"으로 읽음(하이라이트 실패 판정).
  입체감은 면 그라데이션+아랫단(bs-edge)이 담당. 실구현에서도 밑창 면에 하이라이트 얹지 말 것.
- **2026-07-14 5차 확정(사용자 "니가 말한 대로 적용")**: ① 시험 노드 = 잉크 밑창 유지(네이비+금
  깃펜). ② 현재 노드 "시작/학습" 리본 제거 — 워커+펄스 링이 같은 신호를 이미 주므로 중복.
  ③ 골드 리본 2종은 유지·이식: 프리미엄(골드 밑창 #bsGoldGrad+흰 크라운+"프리미엄" 말풍선)·
  정복 인증(골드 밑창+"정복 인증" 말풍선) — trybox 견본(.bsn.prem/.conq + .bs-ribbon, 기존
  gm-ribbon.gold 문법 그대로: 골드 필+꼬리 다이아). ④ 앱바 = 왼쪽 상자(탭 → 전체 화면 과목 허브,
  기존 동작) + 그리드 대신 현재 과목 아이콘+미니 ▾ — 사용자 "오키"로 확정. "과학 ▾" 칩/텍스트안은
  제자리 드롭다운을 암시해 폐기.
- **2026-07-14 6차 반영(사용자 지시+판단 위임)**: ① **열림 노드 = 단원 테마색**(실앱 UNIT_THEME/
  .gm-node.X 규칙 계승 — 목업 Ⅱ 생물 지도의 초록 노드+초록 펄스 링 견본, #bsGradBio·.bsn.bio).
  ② **노드 아이콘 = 레슨 고유 아이콘**(사용자 제안 채택): 열림 흰색, 잠금 회색 예고+우상단 미니
  자물쇠 배지(.bs-lockbadge — "다음에 뭘 배우나" 예고와 잠김을 동시 전달, 큰 자물쇠 단독 표기 폐기),
  완료는 체크 유지(완료 정보가 아이콘보다 우선), 시험 깃펜. .ic-lesson 하나에 CSS가 상태별 색을 입힘
  (잠금 회색→열리면 흰색 — 걷기 데모의 해금 전환이 자동). ③ 워커 위치 문의("옆으로 비켜 아이콘
  보이게?") → **노드 위 유지 판정**: 밟는 스텝 서사의 핵심 장면 + 옆에 서면 소속 노드 모호. 현재
  노드 아이콘 가림은 라벨·워커 존재가 보완(사용자 "굳이 필요 없으면 안 해도" 수용). ④ 기록(구현
  단계 항목): 대단원 탭 줄은 기존처럼 **가로 스와이프 스크롤 유지** — sticky 제안은 세로 스크롤
  고정 얘기라 상충 없음.
- **2026-07-14 7차 반영(사용자 "모두 동의" + 미세 조정)**: ① 미니 자물쇠 배지 확대(r8→9.5, 글리프
  비례 확대). ② **프리미엄 노드는 레슨 아이콘 규칙의 예외** — 골드 밑창+크라운 유지(사용자 확정,
  상품 표지가 레슨 예고보다 우선). ③ **레슨명 라벨을 기존 gm-label 문법으로 복귀**: 반투명 흰 필
  (rgba .78)+블러 4px+필 그림자 — 라벨은 발끝 아래에 떠서 밑창을 가리지 않고, 반투명이라 겹쳐도
  비친다("발바닥 가리려나" 우려 해소). 시험 라벨만 top 3px 올려 지도 하단 클리핑 방지.
- **탭·카드 이중 표기 = 유지 확정**(사용자 "모두 동의"): 탭은 목차(어디로 갈까), 카드는 표지(지금
  여기·정복률) — 급이 다른 요소는 같은 제목이어도 중복으로 안 읽힘. 실구현 시 탭 줄만 세로 sticky
  (가로 스와이프 스크롤은 유지), 카드는 지도와 함께 스크롤로 지나가게.
- **남은 결정: 없음 — 목업 확정(2026-07-14).** 다음 단계 = 실구현(home.ts + ui.css .gm-* 계열):
  이 README 상단 "확정된 방향"과 진행 중 탐색 1~7차 반영이 스펙 정본. (게시본은 7차판으로 갱신.)

## 이미 구현된 것

- **파츠 조합형 스틱맨 아바타**(마이 탭 '직접 꾸미기') — src/ui/stickParts.ts.
  워커 스틱맨은 이 파츠 시스템과 연동 예정("내가 꾸민 스틱맨이 내 진도 위에 선다").
  눈검수 갤러리: dev 서버에서 /qa-stickparts.html.
