// [중2 Ⅵ v3] bodyTeamLab — 스텁(구현 예정). 트리 컴파일 유지용 — 저작 패스에서 교체한다.
import type { StepRenderer } from "../../types";
export const bodyTeamLab: StepRenderer = (_host, _step, api) => {
  api.setCTA("구현 중", { enabled: false });
  return undefined;
};
